<% fifth_data.forEach(function(data) { %>
<div class="center_content">
    <div class="center_content_category">
        <div class="stratch"><p><%= data.stratch_text%></p></div>
    </div>
    <div class="center_text">
        <h4><%= data.text%></h4>
        <p class="small_text"><%= data.dev%> <span>.</span> <%= data.date%></p>
        <p class="text"><%= data.text_bot%></p>
        <button>Read more
            <i class="fas fa-long-arrow-alt-right"></i>
        </button>
    </div>
</div>
<% }) %>